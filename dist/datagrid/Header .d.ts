import { HeaderProps } from '../types/dataGridTypes';
export declare const Header: import('react').MemoExoticComponent<(<T extends {
    id: string | number;
}>({ columns, isAllChecked, onSelectAll, checkboxSelection, sortConfig, onSort, }: HeaderProps<T>) => import("react/jsx-runtime").JSX.Element)>;
